# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.9.0
python-dotenv>=1.0.1

# ── LangChain / LangGraph ─────────────────────────────────────────────────────
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-postgres>=0.0.12
langchain-text-splitters>=0.3.0
langgraph>=0.2.0
langgraph-checkpoint-redis>=0.0.5

# ── LLM / Embeddings ──────────────────────────────────────────────────────────
openai>=1.0.0                  # OpenAI-compatible client for Unsloth Studio API

# ── Database ──────────────────────────────────────────────────────────────────
psycopg[binary]>=3.2.0         # async PostgreSQL driver (psycopg3)
pgvector>=0.3.0                # pgvector Python type support
sqlalchemy>=2.0.0

# ── Redis ─────────────────────────────────────────────────────────────────────
redis[hiredis]>=5.0.0

# ── Retrieval ─────────────────────────────────────────────────────────────────
rank-bm25>=0.2.2               # in-memory BM25 for sparse retrieval
sentence-transformers>=3.0.0   # cross-encoder/ms-marco-MiniLM-L-6-v2 reranker
langchain-huggingface>=0.1.0   # HuggingFaceEmbeddings (replaces langchain-community)
einops>=0.8.0                  # required by nomic-embed-text-v1.5

# ── Token Counting ────────────────────────────────────────────────────────────
tiktoken>=0.8.0                # token counting for context budgeting

# ── Context Compression ───────────────────────────────────────────────────────
llmlingua>=0.2.0               # primary compressor; falls back to summary chain

# ── Observability ─────────────────────────────────────────────────────────────
langfuse>=3.0.0                # LangChain CallbackHandler: from langfuse.langchain import CallbackHandler

# ── Evals ─────────────────────────────────────────────────────────────────────
ragas==0.4.3                    # pinned exact: 0.1.x metric names moved to ragas.metrics.collections in 0.4.x
datasets>=2.20.0               # required by ragas

# ── Data Processing / Ingestion ───────────────────────────────────────────────
pandas>=2.2.0                  # CSV loading for SO + cricket datasets
beautifulsoup4>=4.12.0         # HTML stripping in stackoverflow_loader.py
lxml>=5.0.0                    # bs4 parser backend
numpy>=1.26.0

# ── HTTP / Async ──────────────────────────────────────────────────────────────
httpx>=0.27.0                  # async HTTP (LLM API health checks, GitHub tool)

# ── Tools ─────────────────────────────────────────────────────────────────────
PyGithub>=2.3.0                # backend/tools/github_tool.py

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.66.0

# ── Dev / Linting ─────────────────────────────────────────────────────────────
ruff>=0.8.0
